{
  "name": "agsh",
  "version": "0.1.0",
  "description": "Agent Shell — terminal-native AI agent system",
  "type": "module",
  "bin": {
    "agsh": "./dist/cli.js"
  },
  "files": [
    "dist/",
    "agent.zsh",
    "src/",
    ".env.example"
  ],
  "scripts": {
    "build": "bun build src/cli.ts --outdir=dist --target=node",
    "test": "bun test",
    "verify": "bash test-shell/verify-all.sh",
    "test:shell": "bats test-shell/",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^6.0.3",
    "typescript-language-server": "^5.3.0"
  }
}
